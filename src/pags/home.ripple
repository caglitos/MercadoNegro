import Search from "../organisms/search.ripple";

export default component Home(props: { search: () => void }) {

    <div class="home-container">
        <h1>{"Welcome to the Home Page"}</h1>
        
        <Search action={props.search} />

        <style>
            .home-container {
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                height: 100vh;
                background-color: #f0f0f0;
            }

            h1 {
                margin-bottom: 20px;
            }
        </style>
    </div>
}